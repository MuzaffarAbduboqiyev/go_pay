import 'package:flutter/material.dart';

SizedBox verticalBox({
  required double verticalSize,
}) =>
    SizedBox(
      height: verticalSize,
    );

SizedBox horizontalBox({
  required double horizontalSize,
}) =>
    SizedBox(
      width: horizontalSize,
    );
